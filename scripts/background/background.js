//function onMessage(message, sender, respond) {
//    console.log(message);
//    console.log(sender);
//    respond({});
//};
//chrome.extension.onMessage.addListener(onMessage);
console.log("Background script loaded");

// ?? will find a use for this. Was just going through the documentation and adding stuff.
// I noticed that this continues to run even after the app is closed. Need to find out what this means

